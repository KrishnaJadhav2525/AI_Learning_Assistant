{
    "name": "ai-learning-assistant",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "dev:frontend": "npm run dev --prefix frontend",
        "dev:backend": "python -m uvicorn backend.main:app --reload",
        "dev": "concurrently -c \"cyan,magenta\" \"npm run dev:backend\" \"npm run dev:frontend\""
    },
    "devDependencies": {
        "concurrently": "^8.2.2"
    }
}